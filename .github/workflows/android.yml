name: Build Realistic Battle Royale APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  android:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: |
          sdkmanager "platform-tools"
          sdkmanager "platforms;android-35"
          sdkmanager "build-tools;35.0.0"
          yes | sdkmanager --licenses || true

      - name: Setup Godot
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.5.1
          use-dotnet: false

      - name: Configure Java
        run: |
          echo "JAVA_HOME=$JAVA_HOME_17_X64" >> "$GITHUB_ENV"
          echo "$JAVA_HOME_17_X64/bin" >> "$GITHUB_PATH"

      - name: Configure Android environment
        run: |
          echo "ANDROID_HOME=$ANDROID_SDK_ROOT" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> "$GITHUB_ENV"

      - name: Install Godot export templates
        run: |
          VERSION="4.5.1.stable"
          TEMPLATE_DIR="$HOME/.local/share/godot/export_templates/$VERSION"

          mkdir -p "$TEMPLATE_DIR"
          mkdir -p /tmp/godot-templates

          wget -q \
            "https://github.com/godotengine/godot/releases/download/4.5.1-stable/Godot_v4.5.1-stable_export_templates.tpz" \
            -O /tmp/templates.tpz

          unzip -q \
            /tmp/templates.tpz \
            -d /tmp/godot-templates

          cp \
            /tmp/godot-templates/templates/android_debug.apk \
            "$TEMPLATE_DIR/android_debug.apk"

          cp \
            /tmp/godot-templates/templates/android_release.apk \
            "$TEMPLATE_DIR/android_release.apk"

      - name: Verify project
        run: |
          test -f project.godot
          test -f export_presets.cfg
          test -f scripts/ci_setup.gd
          test -f scenes/Main.tscn

      - name: Configure Godot Android SDK
        run: |
          godot \
            --headless \
            --editor \
            --path . \
            --script res://scripts/ci_setup.gd

      - name: Show Android configuration
        run: |
          echo "ANDROID_HOME=$ANDROID_HOME"
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT"
          echo "JAVA_HOME=$JAVA_HOME"

          echo "Android SDK:"
          ls -la "$ANDROID_SDK_ROOT"

          echo "ADB:"
          "$ANDROID_SDK_ROOT/platform-tools/adb" version || true

          echo "Godot:"
          godot --version

      - name: Create build directory
        run: |
          rm -rf build
          mkdir -p build

      - name: Build Android APK
        run: |
          godot \
            --headless \
            --verbose \
            --path . \
            --export-release "Android" \
            build/RealisticBattleRoyale.apk

      - name: Verify APK
        run: |
          test -f build/RealisticBattleRoyale.apk
          ls -lh build/RealisticBattleRoyale.apk
          file build/RealisticBattleRoyale.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Realistic-Battle-Royale-Android-APK
          path: build/RealisticBattleRoyale.apk
          if-no-files-found: error
          retention-days: 30